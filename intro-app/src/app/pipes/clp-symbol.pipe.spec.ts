import { ClpSymbolPipe } from './clp-symbol.pipe';

describe('ClpSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new ClpSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
