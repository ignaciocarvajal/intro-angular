import { UsdSymbolPipe } from './usd-symbol.pipe';

describe('UsdSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new UsdSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
